import type { ScopedFindOptions } from "@/core/repository/base.repository.types";

// eslint-disable-next-line @typescript-eslint/no-explicit-any
export const DefaultScopedFindOptions: ScopedFindOptions<any> = {
	findOptions: {},
	scopes: [],
};
